name: zz-reusable
on:
  workflow_call:
    inputs:
      msg:
        type: string
        default: hi
jobs:
  r:
    runs-on: ubuntu-latest
    steps:
      - run: echo "reusable said ${{ inputs.msg }}"
